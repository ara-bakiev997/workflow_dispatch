name: Context

on:
  workflow_dispatch:

jobs:
  display_inputs:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ toJson(github) }}"
      - run: echo "${{ steps }}"
      - run: echo "${{ job }}"
